# 前端 Todo

更新时间：2026-05-01
维护方：Codex

## 已完成

- [x] 抖音小程序 TypeScript 项目结构搭建。
- [x] 首页、商品列表、商品详情、预约确认、预约列表、预约详情、在线客服、隐私说明、我的页面完成。
- [x] 商品与预约业务采用 mock 数据跑通。
- [x] 商品内容扩充，区分硬件设备与升级服务。
- [x] SSD 升级类商品支持选择升级配置，并明确提示携带设备到店检测。
- [x] 商品图片已替换为更贴近商品的真实/独立图片，不混用同一图片。
- [x] 不做线上支付，按钮统一为“预约自提/提交升级预约”。
- [x] 客服不放在 Tab 上，改为业务入口触发。
- [x] 在线客服对话页完成基础消息、快捷回复、图片、视频、语音条 UI。
- [x] 去掉文件发送。
- [x] 图片/视频入口收拢到 `+` 面板。
- [x] 语音入口改为左侧麦克风，支持按住说话、上滑取消、松开发送。
- [x] 隐私说明页去掉多余“同意/管理授权”按钮，避免和抖音原生隐私流程冲突。
- [x] 登录策略改为非强制：浏览免登录，客服/预约/个人预约记录按需提示登录。
- [x] `tt.getUserProfile` 仅在用户点击“抖音授权登录”后调用，且 `force: false`。
- [x] 登录成功后自动继续之前动作。
- [x] 整体 UI 按 Semi Design / 抖音官方设计方向完成重构。
- [x] 本地校验通过：TypeScript、JSON、BOM、TTSS 大括号基础校验。
- [x] 2026-05-01 已将前端状态、代码快照 manifest、校验值和联调边界同步到协作仓库。

## 待后端联调前完成

- [ ] 根据 `docs/api-contract.md` v2.1 调整预约提交 payload：后端按 `productId + optionId` 查价格与规格，前端不再提交 `productName/spec/referencePrice` 作为事实字段。
- [ ] 预约提交增加 `Idempotency-Key` header。
- [ ] upgrade 类商品提交预约时确认 `optionId` 必填。
- [ ] 客服 session 创建去掉前端对 `welcomeMessage` 的兼容假设，统一从 `GET /messages` 拉取。
- [ ] 对齐消息字段：主字段统一使用 `id`，`messageId` 仅作为兼容字段。
- [ ] 上传接口联调：确认图片/视频/语音返回 `url/thumbUrl/fileName/fileSize/duration`。
- [ ] 离线消息同步联调：确认 `sync-offline?sessionId=` 行为。
- [ ] 在 `config/env.ts` 填写正式 API 域名并关闭 mock。
- [ ] 在抖音开放平台配置 request/upload/download/socket 合法域名。

## 审核前必须完成

- [ ] 真机扫码测试登录、预约、客服、图片/视频/录音授权流程。
- [ ] 确认抖音开放平台隐私保护指引已生效，并与 `app.json` 权限声明一致。
- [ ] 确认未出现“立即购买、秒杀、最低价、包邮、好评返现”等营销话术。
- [ ] 确认所有价格均标注“门店参考价”。
- [ ] 确认无支付入口、无线上交易表述。
- [ ] 确认客服入口清晰，但不作为底部 Tab。
- [ ] 确认不展示门店地址。
- [ ] 用真实后端环境跑通完整预约链路。

## 当前阻塞

- 本机未安装 `git` / `gh`，当前只能通过 GitHub 连接器同步文档、manifest 和校验值；建议后续安装 Git 后，把完整小程序工程作为标准前端仓库管理。
- 协作仓库不是最终生产代码仓库，完整前端工程当前仍在本地 `D:\dyxcx\自提商城`。
- 后端接口尚未真实联调。
